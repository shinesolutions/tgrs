schema: ../server/src/schema.graphql
generates:
  ./src/shared/__generated__/:
    preset: "client"
    documents: ["src/**/*.{ts,tsx}"]
    config:
      strictScalars: true
      scalars:
        ID: string
